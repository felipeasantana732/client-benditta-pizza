export const loadTrackingScripts = () => {
  
  //So roda essa função se usuário tiver aceito o consentimento  
  
  /* Exemplo de Google Tag
  const gtagScript = document.createElement('script');
  gtagScript.src = 'https://www.googletagmanager.com/gtag/js?id=G-XXXXXXXXXX';
  gtagScript.async = true;
  document.head.appendChild(gtagScript);

  const scriptInline = document.createElement('script');
  scriptInline.innerHTML = `
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'G-XXXXXXXXXX'); // Substitua pelo seu ID real
  `;
  document.head.appendChild(scriptInline);
    */

  // Adicionar o Facebook Pixel, TikTok Pixel, etc., aqui também.
};
